프로세스 : 실해중인 프로그램, 프로그램 실행 시 OS로 부터 자원을 받아 프로세스가 된다.
         프로세스는 프로그램을 수행하는데 필요한 데이터, 메모리 등의 자원, 쓰레드로 구성된다.

멀티태스킹 vs 멀티쓰레딩
 - 멀티태스킹 : 여러개의 프로세스가 동시에 실행
 - 멀티쓰래딩 : 한 프로세스에 여러개의 쓰레드가 동시에 작업
 실제 동시에 처리되는 작업 수는 CPU 코어 개수와 동일. 단 코어가 짧은 시간동안 여러 작업을 번갈아 가며 수행하기 때문에 모두 동시에 실행 되는 것 처럼 보임.

Thread를 실행할때 run() 이 아닌 start()를 호출하는 이유
 - run() 호출은 단순히 Thread를 구현한 클래스에 메서드를 호출 하는 것 main call stack에 run 메서드가 쌓이는 것일 뿐임.
 - start() 호출은 독립적인 작업 수행을 위해 call stack을 새롭게 만들고 run() 메서드를 실행

데몬쓰레드
 - 다른 일반 쓰레드를 돕는 보조적인 역할
 - 일반 쓰레드 모두 종료시 데몬쓰레드는 강제종료 됨.
 - 일반 쓰레드와 작성 및 실행방법 동일 단 실행 전 setDaemon(true) 만 해주면 됨.
 - 데몬쓰레드가 생성한 쓰레드는 자동으로 데몬쓰레드가 됨.